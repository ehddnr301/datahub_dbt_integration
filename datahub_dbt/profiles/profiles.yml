datahub_dbt:
  outputs:
    dev:
      dbname: airflow
      host: postgres
      pass: airflow
      port: 5432
      schema: airflow
      threads: 1
      type: postgres
      user: airflow
  target: dev
